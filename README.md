# 🔐 KernVPN Bot — Telegram Bot для продажи VPN-подписок

[![Python](https://img.shields.io/badge/Python-3.12-blue?logo=python)](https://www.python.org/)
[![aiogram](https://img.shields.io/badge/aiogram-3.0+-green)](https://github.com/aiogram/aiogram)
[![SQLite](https://img.shields.io/badge/SQLite-Database-yellow)](https://www.sqlite.org/)
[![APScheduler](https://img.shields.io/badge/APScheduler-Tasks-red)](https://apscheduler.readthedocs.io/)
[![License](https://img.shields.io/badge/License-MIT-black)](LICENSE)

**KernVPN Bot** — это полнофункциональный Telegram-бот для продажи и управления VPN-подписками на базе AmneziaVPN. Бот поддерживает оплату, управление тарифами, уведомления об истечении подписок и админ-панель.

---

## ✨ Основные возможности

### 👤 Для пользователей:
- ✅ Просмотр тарифов и оформление подписки
- ✅ Профиль с информацией о подписке и статусом
- ✅ Автоматическое получение VPN-ключа
- ✅ Возможность повторного получения ключа
- ✅ Инструкции по установке VPN-приложения
- ✅ Уведомления за 1 день до истечения подписки
- ✅ Автоматическое деактивирование при истечении
- ✅ Продление подписки (сроки суммируются)

### 🔧 Для администратора:
- ✅ Админ-панель с управлением подписками
- ✅ Просмотр всех пользователей и их статусов
- ✅ Список активных подписок с ключами
- ✅ Управление пулом VPN-ключей
- ✅ Принятие/отклонение платежей
- ✅ Статистика по ключам (всего, используется, свободно)
- ✅ Команды для быстрого управления

### ⚙️ Технические особенности:
- 🗄️ **SQLite база данных** — легко развернуть и использовать
- 📅 **APScheduler** — автоматические проверки подписок и уведомления
- 🔐 **Безопасность** — переменные окружения для конфиденциальных данных
- 📱 **Aiogram 3.0** — современный фреймворк для Telegram-ботов
- 🎨 **Удобный интерфейс** — интуитивные кнопки и меню

---

## 📋 Структура проекта

```
kernVPN-bot/
├── app.py                      # Главный файл приложения
├── .env                        # Переменные окружения (не коммитить!)
├── requirements.txt            # Зависимости проекта
│
├── handlers/
│   ├── user_private.py        # Обработчики для пользователей
│   └── admin_private.py        # Обработчики для админа
│
├── kbds/
│   └── keyboards.py           # Клавиатуры и кнопки
│
├── dataBase/
│   └── database.py            # Работа с SQLite БД
│
├── scheduler.py               # Фоновые задачи (напоминания, деактивация)
├── instructions/
│   └── instruction.txt        # Инструкция для пользователей
│
└── .gitignore                 # Исключить из гита
```

---

## 🚀 Установка

### 1. Клонируй репозиторий

```bash
git clone https://github.com/ваше-имя/kernVPN-bot.git
cd kernVPN-bot
```

### 2. Создай виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

### 3. Установи зависимости

```bash
pip install -r requirements.txt
```

### 4. Настрой переменные окружения

Создай файл `.env` в корне проекта:

```env
TOKEN=твой_токен_бота
ADMIN_ID=твой_telegram_id
CARD_NUMBER=номер_твоей_карты
```

**Как получить:**
- **TOKEN**: Напиши боту [@BotFather](https://t.me/BotFather) → `/newbot` → получи токен
- **ADMIN_ID**: Напиши боту [@userinfobot](https://t.me/userinfobot) и узнай свой ID

### 5. Запусти бота локально

```bash
python app.py
```

Если всё в порядке, должен увидеть:
```
✅ База данных инициализирована
📅 Планировщик запущен
🤖 Бот запущен...
```

---

## 📱 Использование бота

### Команды для пользователя:
- `/start` — начало работы
- Кнопки меню: **Тарифы**, **Профиль**, **Инструкция**, **О нас**, **Обратная связь**

### Команды для админа:
- `/admin` — админ-панель
- `/users` — список всех пользователей
- `/active` — список активных подписок с ключами
- `/keystat` — статистика по VPN-ключам
- `/addkey <ключ>` — добавить VPN-ключ в пул

**Пример:**
```bash
/addkey vpn://key123456789abcdef
/addkey vpn://another_key_xyz
```

---

## 💰 Тарифы

| Тариф | Цена | Скидка |
|-------|------|--------|
| 1 месяц | 180 ₽ | — |
| 3 месяца | 486 ₽ | 10% |
| 6 месяцев | 810 ₽ | 25% |

---

## 📊 Логика работы

### Процесс оплаты:

```
Пользователь выбирает тариф
    ↓
Бот показывает номер карты
    ↓
Пользователь отправляет скриншот платежа
    ↓
Админ получает скриншот с кнопками
    ↓
Админ нажимает "Принять" или "Отклонить"
    ↓
Если "Принять" → выдаётся VPN-ключ, активируется подписка
Если "Отклонить" → пользователь получает уведомление
```

### Управление подписками:

```
Каждый день в 10:00 → проверка (за 1 день до истечения)
    → отправка напоминания пользователю

Каждый день в 00:01 → проверка истёкших подписок
    → деактивация (is_active = 0)
    → отправка уведомления об истечении
```

---

## 🐳 Развёртывание на сервер

### Вариант 1: DigitalOcean / Linode

```bash
# 1. Подключись к серверу
ssh root@your_server_ip

# 2. Обнови систему
apt update && apt upgrade -y

# 3. Установи Python
apt install python3-pip python3-venv git -y

# 4. Клонируй проект
git clone https://github.com/ваше-имя/kernVPN-bot.git
cd kernVPN-bot

# 5. Создай .env
nano .env
# Введи: TOKEN=..., ADMIN_ID=..., CARD_NUMBER=...
# Ctrl+X, Y, Enter для сохранения

# 6. Запусти в фоне (tmux или screen)
tmux new-session -d -s bot "python3 app.py"
```

### Вариант 2: Heroku

```bash
# 1. Установи Heroku CLI
# 2. Залогинься
heroku login

# 3. Создай приложение
heroku create kernvpn-bot

# 4. Добавь Config Vars
heroku config:set TOKEN=твой_токен ADMIN_ID=твой_id CARD_NUMBER=номер

# 5. Запушь
git push heroku main
```

### Вариант 3: VPS (Ubuntu 20.04+) с PM2

```bash
# 1. Установи Node.js и PM2
curl -fsSL https://deb.nodesource.com/setup_16.x | sudo -E bash -
sudo apt-get install -y nodejs
sudo npm install -g pm2

# 2. Запусти бота с PM2
pm2 start "python3 app.py" --name kernvpn-bot
pm2 startup
pm2 save
```

---

## 🔧 Настройка для вашего проекта

### Изменение тарифов:

В файле `keyboards.py`:

```python
tariffs_keyboard = InlineKeyboardMarkup(
    inline_keyboard=[
        [InlineKeyboardButton(text="1 месяц — 180₽", callback_data="buy_1")],
        [InlineKeyboardButton(text="3 месяца — 486₽", callback_data="buy_3")],
        # Добавь свои тарифы...
    ]
)
```

И в `user_private.py` функция `buy_tariff_callback`:

```python
prices = {1: 180, 3: 486, 6: 810}  # Измени цены здесь
```

### Изменение текстов:

Все тексты сообщений находятся в `user_private.py` и `admin_private.py`. Просто отредактируй строки с `await message.answer(...)`.

### Добавление новых команд:

Создай новый обработчик в `user_private.py`:

```python
@user_private_router.callback_query(F.data == "my_command")
async def my_command(callback: CallbackQuery):
    await callback.answer()
    await callback.message.answer("Текст ответа", reply_markup=kb.back_menu)
```

---

## 🗄️ База данных

### Таблица `users`:

| Поле | Тип | Описание |
|------|-----|---------|
| user_id | INTEGER | Telegram ID пользователя (первичный ключ) |
| username | TEXT | Username из Telegram |
| is_active | INTEGER | 1 = активна, 0 = неактивна |
| tariff_name | TEXT | Название тарифа (например, "3 месяца") |
| tariff_months | INTEGER | Количество месяцев |
| buy_date | TEXT | Дата покупки (dd.mm.yyyy) |
| end_date | TEXT | Дата истечения (dd.mm.yyyy) |
| price | INTEGER | Цена в рублях |
| vpn_key | TEXT | VPN-ключ AmneziaVPN |
| created_at | TEXT | Дата регистрации |

### Таблица `vpn_keys`:

| Поле | Тип | Описание |
|------|-----|---------|
| key_id | INTEGER | ID ключа (автоинкремент) |
| vpn_key | TEXT | Сам VPN-ключ (уникальный) |
| is_used | INTEGER | 1 = выдан, 0 = свободен |
| assigned_to | INTEGER | ID пользователя, кому выдан |
| assigned_at | TEXT | Дата выдачи |

---

## 📝 Требования

```
aiogram==3.0.0
python-dotenv==1.0.0
APScheduler==3.10.0
```

Установи через:
```bash
pip install -r requirements.txt
```

---

## 🐛 Решение проблем

### Проблема: "OperationalError: no such table: users"
**Решение:** Удали `vpn_bot.db` и перезапусти бота

### Проблема: "TypeError: int() argument must be a string... NoneType"
**Решение:** Проверь `.env` файл, убедись что все переменные заполнены

### Проблема: "TelegramBadRequest: Bad Request: can't parse entities"
**Решение:** Убедись что используешь `parse_mode="HTML"` вместо `parse_mode="Markdown"` или вообще без него

### Проблема: Уведомления не отправляются
**Решение:** Проверь:
1. Бот запущен 24/7
2. Время на сервере правильное (`date`)
3. APScheduler запущен (смотри логи при старте)

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Смотри файл [LICENSE](LICENSE).

---

## 👨‍💻 Автор

**Kern Development**

- Telegram: [@kernvpn_support](https://t.me/kernvpn_support)
- GitHub: [ваш-никнейм](https://github.com/ваш-никнейм)

---

## 🤝 Вклад

Если у тебя есть идеи или найдены баги — создай Pull Request или Issue!

---

## 📞 Поддержка

Если что-то не работает:
1. Проверь логи в консоли
2. Убедись что установлены все зависимости
3. Напиши в поддержку: @kernvpn_support

---

## 🎉 Спасибо за использование KernVPN Bot!

Если проект тебе понравился — ставь ⭐ на GitHub!

```
    ___  ___              _   _______________
   / _ \/ _ | ___  ___  _| | / / ___/ ___/ _ \
  / // / __ |/ _ \/ _ \/ / |/ / /__/ /__/ ___/
 / _  / ___ /  __/ / / / /|  / / _/ /___/ /   
/_//_/_/  |_\___/_/ /_/_/ |_/_/_/ \_____/_/    

```
